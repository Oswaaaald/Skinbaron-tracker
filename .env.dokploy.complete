# =================================================================
# CONFIGURATION DOKPLOY - SkinBaron Alerts
# Copier tout ce contenu dans les variables d'environnement Dokploy
# =================================================================

# === RÉSOLUTION CONFLIT PORTS (OBLIGATOIRE) ===
BACKEND_PORT=3001
FRONTEND_PORT=3002

# === CONFIGURATION BACKEND ===
NODE_ENV=production
PORT=8080

# === DISCORD WEBHOOK (À REMPLACER) ===
DISCORD_WEBHOOK=https://discord.com/api/webhooks/VOTRE_WEBHOOK_ICI

# === API URL FRONTEND (À ADAPTER SELON VOTRE DOMAINE) ===
# Si vous utilisez des domaines Dokploy, remplacer par votre vraie URL
NEXT_PUBLIC_API_URL=https://votre-backend-domain.dokploy.com
# Ou si pas de domaine custom, utiliser l'IP:port
# NEXT_PUBLIC_API_URL=http://IP_SERVEUR:3001

# === CORS (ADAPTER SELON VOTRE FRONTEND) ===
CORS_ORIGIN=https://votre-frontend-domain.dokploy.com
# Ou pour permettre tout (développement seulement)
# CORS_ORIGIN=*

# === DATABASE ===
SQLITE_PATH=/app/data/alerts.db

# === CONFIGURATION SCHEDULER ===
POLL_CRON=*/5 * * * *
ENABLE_BEST_DEALS=true
ENABLE_NEWEST_ITEMS=true

# === FILTRES FEEDS ===
FEEDS_MAX_PRICE=200
FEEDS_MAX_WEAR=0.20

# === API CONFIGURATION ===
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=60000

# === LOGS ===
LOG_LEVEL=info

# === API KEY SKINBARON (OPTIONNEL) ===
# SB_API_KEY=votre_cle_api_si_necessaire