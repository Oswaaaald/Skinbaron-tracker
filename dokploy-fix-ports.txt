# SOLUTION ALTERNATIVE: Configuration Dokploy avec ports internes

# === ESSAYER CETTE CONFIGURATION ===
# Au lieu d'utiliser les ports externes, utiliser les ports internes
# que Dokploy va mapper automatiquement

BACKEND_PORT=8080
FRONTEND_PORT=3000
NODE_ENV=production
PORT=8080
SQLITE_PATH=/app/data/alerts.db
ENABLE_BEST_DEALS=false
ENABLE_NEWEST_ITEMS=false
CORS_ORIGIN=https://app.skinbaron-tracker.oswaaaald.be
NEXT_PUBLIC_API_URL=https://api.skinbaron-tracker.oswaaaald.be
POLL_CRON=*/5 * * * *
FEEDS_MAX_PRICE=200
FEEDS_MAX_WEAR=0.20
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=60000
LOG_LEVEL=info

# EXPLICATION:
# - BACKEND_PORT=8080 (même que PORT=8080)
# - FRONTEND_PORT=3000 (port interne frontend)
# Dokploy devrait automatiquement assigner des ports externes aléatoires