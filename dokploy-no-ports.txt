# SOLUTION RADICALE: Configuration Dokploy SANS variables de port
# Laisser Dokploy gérer les ports automatiquement

NODE_ENV=production
PORT=8080
SQLITE_PATH=/app/data/alerts.db
ENABLE_BEST_DEALS=false
ENABLE_NEWEST_ITEMS=false
CORS_ORIGIN=https://app.skinbaron-tracker.oswaaaald.be
NEXT_PUBLIC_API_URL=https://api.skinbaron-tracker.oswaaaald.be
POLL_CRON=*/5 * * * *
FEEDS_MAX_PRICE=200
FEEDS_MAX_WEAR=0.20
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=60000
LOG_LEVEL=info

# PLUS DE VARIABLES:
# - BACKEND_PORT (supprimé)
# - FRONTEND_PORT (supprimé)

# Dokploy va automatiquement:
# 1. Assigner des ports externes aléatoires
# 2. Configurer Traefik avec les bons ports internes
# 3. Faire le mapping correct