# ================================================================================
# SKINBARON TRACKER - Environment Variables Template
# ================================================================================
# Copy this file to .env and fill in your values
# For deployment platforms (Coolify, Dokploy), set these in platform settings

# ================================================================================
# REQUIRED VARIABLES - Must be set for the application to start
# ================================================================================

# Authentication & Security
JWT_SECRET=                    # Secret key for JWT tokens (min 32 chars recommended)
ENCRYPTION_KEY=                # Encryption key for sensitive data (must differ from JWT_SECRET in production)

# API Configuration
NEXT_PUBLIC_API_URL=           # Public URL of the API (e.g., https://skinbaron-tracker-api.example.com)
CORS_ORIGIN=                   # Allowed CORS origin/Frontend URL (e.g., https://skinbaron-tracker.example.com)

# ================================================================================
# RECOMMENDED VARIABLES - Required for full functionality
# ================================================================================

# SkinBaron Integration
SB_API_KEY=                    # SkinBaron API key (without this, price tracking won't work)

# Cookie Configuration
COOKIE_DOMAIN=                 # Cookie domain for authentication (e.g., .example.com)

# ================================================================================
# OPTIONAL VARIABLES - Have sensible defaults
# ================================================================================

# Environment
NODE_ENV=production            # Environment mode: development | production | test
APP_VERSION=dev                # Application version identifier

# Server
PORT=8080                      # Backend server port

# Database
SQLITE_PATH=/app/data/alerts.db  # Path to SQLite database file

# Polling & Rate Limiting
POLL_CRON=*/5 * * * *          # Cron schedule for price polling (every 5 minutes)
RATE_LIMIT_MAX=1000            # Maximum requests per rate limit window
RATE_LIMIT_WINDOW=60000        # Rate limit window in milliseconds (60s)

# Logging
LOG_LEVEL=info                 # Logging level: error | warn | info | debug

# Compliance
AUDIT_LOG_RETENTION_DAYS=365   # Days to retain audit logs (GDPR compliance)

# ================================================================================
# EXAMPLE PRODUCTION CONFIGURATION
# ================================================================================
# JWT_SECRET=super-secret-jwt-key-change-this-min-32-characters
# ENCRYPTION_KEY=super-secret-encryption-key-different-from-jwt
# NEXT_PUBLIC_API_URL=https://skinbaron-tracker-api.example.com
# CORS_ORIGIN=https://skinbaron-tracker.example.com
# SB_API_KEY=your-skinbaron-api-key
# COOKIE_DOMAIN=.example.com
# NODE_ENV=production
