# CONFIGURATION FINALE - Ports qui évitent TOUS les conflits

# === PORTS EXTERNES (évitent 8080 de Traefik) ===
BACKEND_PORT=3001
FRONTEND_PORT=3002

# === CONFIGURATION BACKEND (port interne) ===
NODE_ENV=production
PORT=8080

# === AUTRES VARIABLES ===
SQLITE_PATH=/app/data/alerts.db
ENABLE_BEST_DEALS=false
ENABLE_NEWEST_ITEMS=false
CORS_ORIGIN=https://app.skinbaron-tracker.oswaaaald.be
NEXT_PUBLIC_API_URL=https://api.skinbaron-tracker.oswaaaald.be
POLL_CRON=*/5 * * * *
FEEDS_MAX_PRICE=200
FEEDS_MAX_WEAR=0.20
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=60000
LOG_LEVEL=info

# EXPLICATION:
# - BACKEND_PORT=3001 (externe, évite conflit avec Traefik:8080)
# - PORT=8080 (interne du container)
# - Les labels Traefik sont dans docker-compose.yml pour forcer port 8080 interne