# SkinBaron Alerts - Configuration pour Dokploy
# Variables d'environnement pour éviter les conflits de ports

# === PORTS (Dokploy) ===
BACKEND_PORT=3001
FRONTEND_PORT=3002

# === BACKEND CONFIGURATION ===
NODE_ENV=production
PORT=8080

# === API KEYS ===
# À configurer dans l'interface Dokploy
SB_API_KEY=

# === DISCORD WEBHOOK ===
# À configurer dans l'interface Dokploy
DISCORD_WEBHOOK=

# === DATABASE ===
SQLITE_PATH=/app/data/alerts.db

# === SCHEDULER ===
POLL_CRON=*/5 * * * *
ENABLE_BEST_DEALS=true
ENABLE_NEWEST_ITEMS=true

# === FEEDS FILTERS ===
FEEDS_MAX_PRICE=200
FEEDS_MAX_WEAR=0.20

# === API CONFIGURATION ===
CORS_ORIGIN=*
RATE_LIMIT_MAX=100
RATE_LIMIT_WINDOW=60000

# === LOGGING ===
LOG_LEVEL=info

# === FRONTEND ===
# Remplacer par votre vraie URL de backend Dokploy
NEXT_PUBLIC_API_URL=http://localhost:3001